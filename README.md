# BTC Market Maker
Set-up:

Download and extract
Open command prompt (Win + cmd + Enter)
Type cd "C:\Users[REPLACE WITH ACTUAL PATH TO FOLDER]\btc_market_maker-main\btc_market_maker-main"
Type py -m pip install -r requirements.txt
One-command way to run:

Type py main.py
--- To run tests ---

Open command prompt (Win + cmd + Enter)
Type cd "C:\Users[REPLACE WITH ACTUAL PATH TO FOLDER]\btc_market_maker-main\btc_market_maker-main"
Type py -m pip install pytest
One-command way to run:

Type py -m pytest
